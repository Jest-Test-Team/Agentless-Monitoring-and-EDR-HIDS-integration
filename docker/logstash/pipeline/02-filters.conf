filter {
  # ── DRAKVUF VMI events ──
  if [type] == "drakvuf" or [fields][log_type] == "drakvuf" {
    mutate {
      rename => {
        "TimeStamp" => "@timestamp"
        "VMName" => "[host][name]"
        "ProcessId" => "[process][pid]"
        "ParentProcessId" => "[process][parent][pid]"
        "Image" => "[process][executable]"
        "CommandLine" => "[process][command_line]"
        "EventId" => "[event][code]"
      }
    }

    # Map EventId to ECS categories
    if [event][code] == 1 {
      mutate { add_field => { "[event][category]" => "process" "[event][type]" => "start" } }
    } else if [event][code] == 8 {
      mutate { add_field => { "[event][category]" => "process" "[event][type]" => "info" } }
    } else if [event][code] == 11 {
      mutate { add_field => { "[event][category]" => "file" "[event][type]" => "creation" } }
    }

    # Sanitize
    mutate {
      gsub => ["[process][command_line]", "(-p\\s+)\\S+", "\\1[REDACTED]"]
      remove_field => ["[process][environment]", "[memory][buffer]"]
    }

    date { match => ["@timestamp", "ISO8601"] }

    mutate { add_field => { "[labels][tier]" => "tier0" } }
  }

  # ── Suricata NIDS events ──
  if [type] == "suricata" or [fields][log_type] == "suricata" {
    mutate {
      rename => {
        "[src_ip]" => "[source][ip]"
        "[src_port]" => "[source][port]"
        "[dest_ip]" => "[destination][ip]"
        "[dest_port]" => "[destination][port]"
        "[proto]" => "[network][protocol]"
        "[alert][signature]" => "[event][reason]"
        "[alert][severity]" => "[event][severity]"
      }
      add_field => { "[event][category]" => "network" }
    }
  }

  # ── Common enrichment ──
  # Correlation fingerprint
  fingerprint {
    source => ["[host][name]", "[process][pid]", "@timestamp"]
    target => "[event][hash]"
    method => "SHA256"
  }

  # Add observer info
  mutate {
    add_field => {
      "[observer][product]" => "agentless-defense"
      "[observer][vendor]"  => "opensource-stack"
      "[event][kind]"       => "security"
    }
  }
}
